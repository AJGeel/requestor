CREATE TABLE experience (
    id int(11) NOT NULL AUTO_INCREMENT,
    user_id int(11),
    experience_level int(1),
    novelty_familiarity int(1),
    novelty_frequency int(1),
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY(id)
)
